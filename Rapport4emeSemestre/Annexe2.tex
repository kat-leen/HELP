\begin{lstlisting}
CvCapture* capture;
cv::Mat frame;
capture = cvCaptureFromCAM(0);
	if (capture) {
		while (true) {
			frame = cvQueryFrame(capture);
			// mirror it
			cv::flip(frame, frame, 1);
			frame.copyTo(debugImage);
\end{lstlisting}